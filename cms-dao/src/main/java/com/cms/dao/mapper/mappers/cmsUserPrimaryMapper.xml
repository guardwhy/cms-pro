<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
实体类的映射文件: namespace 指定接口的类全名
-->
<mapper namespace="com.cms.dao.mapper.CmsUserPrimaryMapper">
    <!--查询sql-->
    <sql id="baseColumn">
        id,
        username,
        password,
        salt,
        email,
        status,
        deleted
    </sql>
    
    <resultMap id="baseResultMap" type="com.cms.dao.entity.CmsUserPrimaryEntity">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="salt" column="salt"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <!--查询-->
    <select id="getByUsername" resultMap="baseResultMap">
        select <include refid="baseColumn"/> from from cms_user_primary where username = #{username}
    </select>
</mapper>